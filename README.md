# English-To-Persian-Translate
📚 مترجم انگلیسی به فارسی (English-To-Persian-Translate)
این پروژه یک اسکریپت پایتون سبک و کارآمد برای ترجمه فوری کلمات انگلیسی به فارسی است. تمرکز اصلی آن بر روی ادغام آسان با برنامه‌های شخص ثالث، به خصوص پخش‌کننده‌های ویدئو مانند Potplayer، برای ارائه ترجمه سریع کلمات در حین تماشای محتوا است. این نسخه در حال حاضر تنها با سیستم‌عامل ویندوز سازگار است.
✨ ویژگی‌ها
 * ترجمه فوری کلمات: به سرعت کلمات انگلیسی را به معادل فارسی‌شان ترجمه می‌کند.
 * سبک و کم‌حجم: طراحی شده برای عملکرد بهینه و مصرف حداقل منابع.
 * یکپارچه‌سازی آسان: به راحتی می‌توان آن را با برنامه‌های دیگر (مانند Potplayer) ادغام کرد.
 * سازگار با ویندوز: توسعه یافته برای محیط ویندوز.
⚙️ پیش‌نیازها
قبل از استفاده از این اسکریپت، مطمئن شوید که موارد زیر روی سیستم شما نصب شده‌اند:
 * پایتون 3: ترجیحاً پایتون 3.6 به بالا. می‌توانید آن را از python.org دانلود کنید.
 * PIP: معمولاً با نصب پایتون همراه است.
 * کتابخانه‌های پایتون:
   pip install googletrans==4.0.0rc1 # یا هر نسخه پایدار دیگری از googletrans

   نکته: به دلیل تغییرات احتمالی در API گوگل ترنسلیت، ممکن است نیاز باشد نسخه googletrans را با دقت انتخاب کنید یا از جایگزین‌های آن استفاده کنید.
🚀 نصب و راه‌اندازی
 * کلون کردن مخزن:
   ابتدا، این مخزن را به سیستم محلی خود کلون کنید:
   git clone https://github.com/sepehr-dev-git/English-To-Persian-Translate.git

 * ورود به دایرکتوری پروژه:
   cd English-To-Persian-Translate

 * نصب وابستگی‌ها:
   با فرض اینکه فایل اصلی اسکریپت شما EnToFa.py نام دارد و وابستگی‌ها در requirements.txt لیست شده‌اند (اگر نه، pip install googletrans==4.0.0rc1 را اجرا کنید):
   pip install -r requirements.txt

💻 نحوه استفاده
این اسکریپت را می‌توان به صورت مستقل اجرا کرد یا با برنامه‌های دیگر ادغام نمود.
۱. استفاده مستقل (خط فرمان)
می‌توانید اسکریپت را مستقیماً از طریق خط فرمان اجرا کرده و کلمه مورد نظر را به عنوان آرگومان به آن بدهید:
python EnToFa.py "hello"

یا
python EnToFa.py "apple"

خروجی باید ترجمه فارسی کلمه را نمایش دهد.
۲. ادغام با Potplayer
یکی از کاربردهای اصلی این اسکریپت، ادغام آن با Potplayer برای ترجمه سریع کلمات در حین پخش ویدئو است. مراحل زیر را دنبال کنید:
 * فایل EnToFa.py را آماده کنید: مطمئن شوید که اسکریپت EnToFa.py (یا هر نامی که برای فایل اصلی اسکریپت ترجمه خود انتخاب کرده‌اید) در مسیری قابل دسترس قرار دارد.
 * باز کردن Potplayer: Potplayer را باز کنید.
 * راست کلیک روی کلمه: در Potplayer، هنگام پخش ویدئو، روی کلمه انگلیسی مورد نظر خود (مثلاً در زیرنویس) راست کلیک کنید.
 * انتخاب "Customize": از منوی باز شده، گزینه Customize را انتخاب کنید.
 * افزودن آیتم جدید: در پنجره Customize، روی دکمه Add کلیک کنید.
 * پیکربندی آیتم: در پنجره Edit Menu Item، اطلاعات زیر را وارد کنید:
   * Title (عنوان): Translator (یا هر نام دلخواه دیگر)
   * Path (مسیر):
     C:\Path\To\Your\Python\python.exe C:\Path\To\Your\Project\EnToFa.py "%%SS"
     * توضیحات مسیر:
       * C:\Path\To\Your\Python\python.exe: مسیر کامل فایل اجرایی python.exe شما.
       * C:\Path\To\Your\Project\EnToFa.py: مسیر کامل اسکریپت EnToFa.py در پروژه شما.
       * "%%SS": این یک متغیر داخلی Potplayer است که کلمه‌ای که روی آن راست کلیک کرده‌اید را به اسکریپت شما ارسال می‌کند.
 * تأیید و ذخیره: روی Ok کلیک کنید تا آیتم منو اضافه شود.
 * ترجمه کلمات: حالا، با راست کلیک روی هر کلمه انگلیسی در Potplayer و انتخاب Translator از منو، ترجمه فارسی آن در قالب یک نوتیفیکیشن Potplayer به شما نمایش داده می‌شود.
🤝 مشارکت
از هرگونه مشارکت برای بهبود این پروژه استقبال می‌شود! اگر ایده‌ای برای ویژگی جدید دارید، باگی پیدا کرده‌اید یا می‌خواهید کد را بهینه‌سازی کنید، لطفاً:
 * مخزن را فورک (Fork) کنید.
 * یک شاخه جدید (Branch) برای تغییرات خود ایجاد کنید (git checkout -b feature/AmazingFeature).
 * تغییرات خود را کامیت (Commit) کنید (git commit -m 'Add some AmazingFeature').
 * شاخه خود را به مخزن اصلی پوش (Push) کنید (git push origin feature/AmazingFeature).
 * یک درخواست کشش (Pull Request) باز کنید.

